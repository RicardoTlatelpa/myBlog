import React, { Fragment } from "react"
import { Styled } from "theme-ui"

/**
 * Change the content to add your own bio
 */

export default () => (
  <Fragment>
     <Styled.a href="http://github.com/ricardotlatelpa">Ricardo Tlatelpa</Styled.a> 
     <br/>
     Having fun with code.
  </Fragment>
)
